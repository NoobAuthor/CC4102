// utils.h
// Utilidades generales. Funcionalidad de grafos será implementada en Python.
#pragma once
#include <string>
#include <vector>

/**
 * Divide una cadena por espacios (útil para parsing).
 * @param str Cadena a dividir.
 * @return Vector de tokens.
 */
std::vector<std::string> split(const std::string& str);
